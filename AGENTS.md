# Repository Instructions

## Git workflow

- 默认直接在当前工作目录的 `main` 分支开发、测试和提交，不创建额外的 Git worktree。
- 需要隔离开发时，可以创建一个由用户和 AI 共同使用的 `feat` 分支；整个项目同时最多保留一个 `feat` 分支。
- 创建或切换到 `feat` 分支前先确认当前没有其他 `feat` 分支，并向用户说明。
- 除非用户明确要求，不创建额外 worktree，也不为每个任务创建独立分支。
